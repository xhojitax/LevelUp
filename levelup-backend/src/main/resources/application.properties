# Application Name
spring.application.name=LevelUp-Backend

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/levelup_local?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=levelup_dev
spring.datasource.password=LevelUp2024!Dev
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# Swagger/OpenAPI Configuration
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs

# JWT Configuration
jwt.secret=LevelUpSecretKeyForJWT2024MustBeLongEnoughForHS256AlgorithmSecurity
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000

# Logging
logging.level.com.levelup.backend=DEBUG